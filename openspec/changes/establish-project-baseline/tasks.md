## 1. 产品决策与模块地图

- [x] 1.1 新建 docs/requirements-map.md，建立已确认决策及 D01–D10 台账，区分四类决策状态与实现状态；核对 design 中每项已确认结论、问题及阻塞范围均可追踪。
- [x] 1.2 按依赖顺序逐项发起尚未确认的产品问题，记录用户原意与回答来源；核对只有明确回答的条目转为已确认，未回答项保留待决定及受影响切片，本项以确认过程和状态记录完整为完成标准，不要求替用户作出所有决定。
- [x] 1.3 修订 docs/PRODUCT_BRIEF.md，落实取消收藏/旧内容回顾、单工作区、视频上传、主动重复保存、字符串标签及频度词云；逐项对照 project-baseline 的产品一致性场景，待决定项只引用台账。
- [x] 1.4 在 requirements-map 补齐十个逻辑模块的职责、主要流程、依赖、范围、验收方向及推荐开发切片；核对 Note 闭环与媒体上传分别具有独立依赖和进入条件，不把推荐顺序写成已执行计划。

## 2. 架构、采集与媒体文档

- [x] 2.1 填充 docs/architecture.md，说明当前脚手架与目标结构、身份/内容/工作区/标签/媒体概念模型及两端到后端的数据流；核对所有权边界明确，拟议 Schema 与既有源码分开标记。
- [x] 2.2 新建 docs/capture-contract.md，记录三类采集 payload 草案、结果和错误分类、tags 字符串、身份校验、新操作与重试 ID 语义；以网页新存、同操作重试、未登录、错误工作区四个文档示例验证，并列出与当前 CreateInspirationInput 的差异。
- [x] 2.3 核实官方媒体存储资料并新建 docs/media-storage.md，比较 Convex Storage、私有对象存储和必要时的视频处理服务；核对引用与查阅日期、推荐/待决定状态、访问隐私取舍和 D02/D03 引用齐全，不自动选购或配置服务。
- [x] 2.4 在媒体文档补齐上传授权、完成核验、所有权、失败/取消、清理、图片转存和视频播放生命周期；逐项检查 D01/D04 等未定行为及认证、大文件、受保护来源等实验的输入、预期证据和阻塞范围。

## 3. 设计依据与运行验证说明

- [x] 3.1 新建 docs/design/README.md，映射五份原型、导航入口、用户流程及适用数据状态；核对收藏数量、Landing 收藏措辞和静态标签布局都标为失效或受限参考，不改 HTML 或自选替代指标。
- [x] 3.2 新建 docs/design/tokens.md，从原型提取颜色、字号、间距参考并说明主题与词云强调目标；核对每项的来源和确认状态，未批准的参数不冒充正式 Token，现有主题算法不被假定已存在。
- [x] 3.3 新建 docs/development.md，按实际 package scripts 记录安装、Web/插件启动和现有检查入口，列出后端与环境配置待建设范围；核对包名、路径和命令，所有秘密值使用占位说明，不创建环境文件或暴露本机凭证。
- [x] 3.4 新建 docs/verification.md，建立契约、两端、用户隔离、媒体、搜索/统计/词云与集成流程的验证矩阵；核对根占位 test、空 CI/Vitest 配置、缺失 typecheck 均被明确标识，并区分文档审阅、自动测试和人工 Chrome 验证。

## 4. 代理协作规则与 OpenSpec 配置

- [x] 4.1 修订 docs/ENGINEERING_HARNESS.md，增加文档责任地图和 explore/propose/apply/归档分工，将重复采集字段表改为契约引用；核对没有独立重复任务模板、旧 tagIds 要求或业务已实现声明。
- [x] 4.2 修订根 AGENTS.md，保留简短全仓边界、按任务阅读入口、真实验证命令和提案/实施边界，同步字符串标签与无收藏等约定；核对引用目标已存在且没有要求读取全部文档才能处理小任务。
- [x] 4.3 新建 apps/web/AGENTS.md 与 apps/extension/AGENTS.md，仅补 UI 状态与主题、MV3/消息/权限/重启等局部要求；对照根规则消除重复与冲突，明确现有广泛域名权限只是待收敛配置。
- [x] 4.4 根据本地 OpenSpec 1.12.0 支持的配置结构更新 openspec/config.yaml，加入简短上下文与产物定制，保留默认 schema；通过只读 openspec instructions 检查加载效果和结构，确认未修改生成技能、未引入自定义 schema 或重复产品规则。

## 5. 跨文档验收与交接

- [x] 5.1 审阅全部目标文档的相对链接、责任边界和决策 ID，搜索收藏/旧内容回顾/tagIds 等词并逐项确认仅存在于明确的移除说明或失效参考；依据两份 spec 的每个场景记录通过或缺口。
- [x] 5.2 检查媒体引用、当前脚本事实、实现状态及未定项在各文档中一致；核对每个阻塞决策都有受影响切片且未获得回答的内容没有被默认为确认。
- [x] 5.3 执行 openspec validate establish-project-baseline --strict，并审查本次编辑范围；确认只有 proposal 指定的文档/配置和本 change 进度记录发生变化，没有修改源码、原型、测试/CI、依赖或用户其他文件。
- [x] 5.4 交付文档索引、实际检查结果、未解决事项与后续编码入口建议；核对明确区分“文档基线可审阅”和“业务可以实施”，不自动启动 establish-engineering-checks、业务 change 或归档。

## 执行记录（2026-09-06）

- 已交付 proposal 约定的 14 份文档/配置；原型与源码保持原状。
- 用户明确确认直接删除、媒体详情浮层、短时访问、图片/视频上传范围；同步 requirements-map 的 U3–U6。
- 1.2 完成指逐项确认过程与状态记录完整，不代表所有问题定案。当前 D03b 已提问待回复，D03c/D04–D10 待后续逐项询问，不以沉默视为同意。
- 文档链接、规则加载、跨文档场景审阅、范围哈希与 OpenSpec 严格校验通过；详细记录见 docs/verification.md。
- 应用测试、浏览器/媒体实验未执行；未开始后续代码 change、部署或归档。
